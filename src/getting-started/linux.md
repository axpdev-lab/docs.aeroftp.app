# Linux Installation

AeroFTP is distributed as `.deb`, `.rpm`, `.AppImage`, `.snap`, and via AUR (`aeroftp-bin`). Download the appropriate package from GitHub Releases or install from your package manager.
